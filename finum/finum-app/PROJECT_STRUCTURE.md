# Project Structure - Finum App

Structure complète du projet après Sprint 0 & 1.

## 📁 Root Files

```
finum-app/
├── package.json              # Dependencies & scripts
├── package-lock.json         # Lock file
├── tsconfig.json             # TypeScript config
├── next.config.ts            # Next.js config
├── tailwind.config.ts        # Tailwind config
├── postcss.config.mjs        # PostCSS config
├── eslint.config.mjs         # ESLint config
├── .nvmrc                    # Node version (v24.3.0)
├── .gitignore                # Git ignore patterns
├── .env.example              # Env variables template
└── .env.local                # Env variables (git-ignored)
```

## 📚 Documentation

```
├── README.md                 # Main overview
├── GETTING_STARTED.md        # Quick start guide
├── SETUP_GUIDE.md            # Complete setup
├── QUICK_START.md            # 5-minute start
├── COMMANDS.md               # Commands reference
├── TEST_SPRINT_1.md          # Test guide
├── STATUS.md                 # Quick status
├── SPRINT_0_COMPLETE.md      # Sprint 0 details
├── SPRINT_1_COMPLETE.md      # Sprint 1 details
├── FILES_CREATED.md          # All files list
├── IMPLEMENTATION_COMPLETE.md # Global overview
└── PROJECT_STRUCTURE.md      # This file
```

## 🗄️ Database

```
prisma/
└── schema.prisma             # 8 models (User, Budget, Transaction, etc.)
```

## 🎨 Frontend - Pages

```
src/app/
├── layout.tsx                # Root layout (fonts)
├── page.tsx                  # Home (redirect /cockpit)
├── globals.css               # Global styles
│
├── (auth)/                   # Auth route group
│   └── login/
│       └── page.tsx          # Login page (Google SSO)
│
└── (dashboard)/              # Protected route group
    ├── layout.tsx            # Dashboard layout
    ├── cockpit/
    │   └── page.tsx          # Dashboard metrics
    ├── budget/
    │   └── page.tsx          # Budget management
    ├── transactions/
    │   └── page.tsx          # Transactions table
    ├── patterns/
    │   └── page.tsx          # Patterns detection
    └── coach/
        └── page.tsx          # Weekly review
```

## 🔌 Backend - API Routes

```
src/app/api/
├── auth/
│   └── [...nextauth]/
│       └── route.ts          # NextAuth handlers
├── budget/
│   ├── route.ts              # GET/POST budgets
│   └── import/
│       └── route.ts          # POST import CSV
├── transactions/
│   ├── route.ts              # GET/POST transactions
│   └── import/
│       └── route.ts          # POST import CSV
└── cockpit/
    └── route.ts              # GET metrics
```

## 🧩 Components

```
src/components/
├── layout/
│   ├── DashboardNav.tsx      # Side navigation
│   └── DashboardHeader.tsx   # Top header + user menu
│
├── dashboard/
│   ├── BudgetImportModal.tsx      # Budget CSV import
│   ├── TransactionImportModal.tsx # Transaction CSV import
│   ├── MetricCard.tsx             # Metric display card
│   └── BudgetProgressCard.tsx     # Budget progress bar
│
├── charts/
│   └── SpendingTrendChart.tsx     # 6-month trend chart
│
└── ui/                       # shadcn/ui components
    └── (generated by shadcn)
```

## 🔧 Business Logic

```
src/lib/
├── auth.ts                   # NextAuth config
├── prisma.ts                 # Prisma client
├── utils.ts                  # shadcn utils
│
├── parsers/
│   ├── csv-parser.ts         # Generic CSV parser
│   └── transaction-parser.ts # Transaction parser
│
├── services/
│   ├── budget.service.ts     # Budget CRUD
│   ├── transaction.service.ts # Transaction CRUD
│   ├── runrate.service.ts    # Metrics calculations
│   └── (to come: bucket, rule, pattern services)
│
└── utils/
    └── currency.ts           # FX conversion
```

## 🔐 Middleware

```
src/
└── middleware.ts             # Route protection
```

## 📊 Sample Data

```
sample-data/
├── README.md                           # Usage guide
├── example-budgets.csv                 # 10 budgets
├── example-transactions.csv            # 19 transactions
└── example-transactions-multi-currency.csv  # 9 FX transactions
```

## 📦 Generated Files (git-ignored)

```
.next/                        # Next.js build
node_modules/                 # Dependencies (494 packages)
*.tsbuildinfo                 # TypeScript build info
next-env.d.ts                 # Next.js types
```

## 📈 Statistics

- **Total files (source)**: 42
- **Total files (with node_modules)**: 494+ packages
- **Lines of code**: ~3300
- **Documentation files**: 12
- **Sample CSV files**: 3
- **React components**: 9
- **API endpoints**: 5
- **Services**: 4
- **Parsers**: 2

## 🎯 Key Files by Feature

### Authentication
- `src/lib/auth.ts` - NextAuth config
- `src/middleware.ts` - Route protection
- `src/app/api/auth/[...nextauth]/route.ts` - Auth endpoints
- `src/app/(auth)/login/page.tsx` - Login UI

### Import CSV
- `src/lib/parsers/csv-parser.ts` - Generic parser
- `src/lib/parsers/transaction-parser.ts` - Transaction specific
- `src/app/api/budget/import/route.ts` - Budget import endpoint
- `src/app/api/transactions/import/route.ts` - Transaction import endpoint
- `src/components/dashboard/*ImportModal.tsx` - Import UIs

### Dashboard Metrics
- `src/lib/services/runrate.service.ts` - Calculations
- `src/app/api/cockpit/route.ts` - Metrics endpoint
- `src/app/(dashboard)/cockpit/page.tsx` - Dashboard UI
- `src/components/dashboard/MetricCard.tsx` - Metric display
- `src/components/charts/SpendingTrendChart.tsx` - Chart

### Business Logic
- `src/lib/services/budget.service.ts` - Budget operations
- `src/lib/services/transaction.service.ts` - Transaction operations
- `src/lib/utils/currency.ts` - FX conversion

### Database
- `prisma/schema.prisma` - Schema definition (8 models)
- `src/lib/prisma.ts` - Client singleton

### UI Components
- `src/components/layout/*` - Layout components
- `src/components/dashboard/*` - Business components
- `src/components/charts/*` - Chart components
- `src/components/ui/*` - shadcn base components

## 🔍 Finding Code

### "Where is the CSV parsing logic?"
→ `src/lib/parsers/csv-parser.ts` + `transaction-parser.ts`

### "Where are the metrics calculated?"
→ `src/lib/services/runrate.service.ts`

### "Where is the budget import handled?"
→ API: `src/app/api/budget/import/route.ts`
→ UI: `src/components/dashboard/BudgetImportModal.tsx`

### "Where is the authentication configured?"
→ Config: `src/lib/auth.ts`
→ Middleware: `src/middleware.ts`
→ API: `src/app/api/auth/[...nextauth]/route.ts`

### "Where are the database models?"
→ `prisma/schema.prisma`

### "Where is the dashboard UI?"
→ `src/app/(dashboard)/cockpit/page.tsx`

## 📝 Notes

- All `.tsx` files are React components (frontend)
- All `route.ts` files are API endpoints (backend)
- All `.service.ts` files are business logic (backend)
- All files in `src/components/` are reusable UI components
- All files in `src/lib/` are utilities and business logic
- Sample data is in `sample-data/` for easy testing

---

**Navigate easily! 🗺️**
